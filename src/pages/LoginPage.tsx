import AppTemplate from 'templates/AppTemplate';

export default function LoginPage(): JSX.Element {
  return (
    <div>
      <AppTemplate>
        <h1>Login Page</h1>
      </AppTemplate>
    </div>
  );
}
