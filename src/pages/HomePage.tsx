import AppTemplate from 'templates/AppTemplate';

export default function HomePage(): JSX.Element {
  return (
    <div>
      <AppTemplate>
        <h1>Home Page</h1>
      </AppTemplate>
    </div>
  );
}
